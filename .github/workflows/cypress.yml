name: Cypress

on: [push, pull_request]

jobs:
  test:
    name: Run Cypress tests
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: true

    - name: Setup node
      uses: actions/setup-node@v6
      with:
        node-version: 24

    - name: Setup cache (carton)
      uses: actions/cache@v5
      with:
        path: local
        key: ubuntu-24-carton-${{ hashFiles('cpanfile.snapshot') }}

    - name: Setup cache (cypress)
      uses: actions/cache@v5
      with:
        path: |
          ~/.npm
          node_modules
        key: ubuntu-24-node-24-cypress-15.14.2-coverage

    - name: Install packages
      run: |
        sudo apt update
        sudo apt install -y gettext jhead language-pack-en libreadline-dev libpng-dev
        npm install cypress@15.14.2 @cypress/code-coverage@4.0.3
        vendor/bin/carton install --deployment
        commonlib/bin/gettext-makemo FixMyStreet
        echo "$(npm bin)" >> $GITHUB_PATH

    - name: Run Cypress tests
      run: |
        bin/browser-tests --coverage run ${CYPRESS_RECORD_KEY:+--record}
      env:
        CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}

    - name: Upload code coverage results
      if: success()
      continue-on-error: true
      uses: Wandalen/wretry.action@master
      with:
        action: codecov/codecov-action@v3
        with: |
          files: ./coverage/lcov.info
          fail_ci_if_error: true
        attempt_limit: 5
        attempt_delay: 10000
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

#    - name: Archive code coverage results
#      uses: actions/upload-artifact@v2
#      with:
#        name: code-coverage-report
#        path: coverage/lcov-report
